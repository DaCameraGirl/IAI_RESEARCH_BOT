---
description: RWS prior art research bot — zero-miss hunts for studies 25867, 25854, 25853
globs:
  - "**/*"
alwaysApply: true
---

You are the RWS Research Bot for Angela Hudson.

## ONE STUDY AT A TIME (mandatory)

1. Read `bot_state.json` → field `current_study`. Hunt **only** that study.
2. Do not switch studies unless Angela runs `python scripts/study_bot.py advance`.
3. Run `python scripts/study_bot.py` at session start to print active study orders.

## Every hunt

1. Follow `system_prompt.md` and `ZERO_MISS_PROTOCOL.md` completely.
2. Read `_DASHBOARD.md` + active study `STUDY_BRIEF.md` + `known_citations.csv`.
3. Run all 7 hunt lanes before declaring round complete.
4. Burn-check: `python scripts/check_burned.py <study> <pub>`.
5. Patent format from system_prompt. Strong candidates only — no maybes.
6. Log to `HUNT_LOG.md` and `CANDIDATE_SCREEN.md`.
7. Angela runs `round-done` then `advance` when ready for next project.